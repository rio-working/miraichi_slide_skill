# ブラッシュアップ作業 中断時点の状態（2026-07-11 01:13 JST）

ユーザー指示により一旦停止。01:30に再開予定。

## 完了済み（7件の問題すべて修正実装済み・未検証）

1. **デフォルトカラー統一** ✅ — 「モノトーンデフォルト」→「template.js標準カラー（00A495/FABF00）がデフォルト、モノトーンは明示指定時のみ」に統一。
   変更: `CLAUDE.md`(4箇所+プリセット期待値注記)、`.claude/agents/design-template-planner.md`、`template-generator.md`、`brand-researcher.md`(L173)、`.claude/workflows/design-template_workflow.md`(3箇所)、`.claude/skills/design-template/SKILL.md`(手順2)
2. **22→30表記統一** ✅ — `slide-architect.md` L4、`CLAUDE.md` L8、`scripts/test.js` L14/17/19
3. **pattern-reference.md** ✅ — パターン23〜30の定義を追記（slide-scripter.mdと同内容、日本語形式）
4. **提案書モード引き継ぎ** ✅ — `slide-builder-planner.md` に mode / case_type / plan_names 入力と「提案書モードの前提条件」節を追加。Step 0 の分岐も標準カラー方針に修正
5. **HTML色ハードコード** ✅ — `slide-scripter.md` Phase 2.5 手順3、`html-export.md` 冒頭に「template.jsのCOLORS現在値を動的反映」を明記
6. **配信フロー一本化** ✅ — `slide-builder_workflow.md` Step 3 とPath Conventionに HTML/Vercel 経路追記。`slide-scripter.md` Phase 4 に「提案書モード×HTML出力の場合」節を追加
7. **プリセット期待値** ✅ — `CLAUDE.md` Step 0 に注記追加（1社のみ・色再現精度・プリセット保存推奨）

検証済み: `node scripts/test.js` 成功（全30パターン生成OK、output/test_all_patterns.pptx）

## コードレビューで発見したバグ（未修正・次の作業）

**`scripts/presets/index.js` の `apply()` — CHART_COLORS と FACE が実際には反映されない**

- 実証済み: 偽プリセット（chartColors FF0000系）を apply → `t.CHART_COLORS[0]` は FF0000 になるが、
  生成されたチャートXML内の色は `00A495` のまま（template.js内部のモジュールスコープ変数
  `CHART_COLORS` / `FACE` は関数内から参照され、exports側の差し替えでは変わらないため）
- `COLORS` は同一オブジェクトのプロパティ書き換えなので正常に反映される（docomo適用でCC0033確認済み）

### 修正方針（最小差分）
1. `presets/index.js` apply(): `template.CHART_COLORS = preset.chartColors;` を
   in-place置換に変更（`length=0` → `push`）→ template.js変更不要で直る
2. FACE: `template.js` に `setFace(name){ FACE = name; }` を追加してexport、
   apply() から `template.setFace && template.setFace(preset.font)` を呼ぶ
3. 修正後、偽プリセットテストを再実行してチャートXMLに新色が入ることを確認

## 残タスク

- [ ] presets.apply() バグ修正＋再検証（上記）
- [ ] 全修正箇所の最終Read検証
- [ ] 作業報告書（朝確認用）の作成: 修正一覧・証拠・コードレビュー所見
- [ ] `.claude/revision_log.md`（リポジトリルート）にバグパターン1行追記
